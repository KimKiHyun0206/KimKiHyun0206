name: Latest blog post workflow
on:
  schedule:
    # 매일 한국 시간(KST) 00:00에 업데이트 (UTC 15:00)
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Pull in posts
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://v2.velog.io/rss/@duskafka"
          # 1. 대신 <li>를 사용하여 <ol> 태그의 자동 번호 기능을 활용합니다.
          # style을 통해 파란색 링크와 밑줄을 제거합니다.
          template: "<li><a href='$url' style='color: #DDE6ED; text-decoration: none;'>📖 $title</a> <span style='color: #DDE6ED;'>($date)</span></li>"
          date_format: "yyyy-mm-dd"
          max_post_count: 10
