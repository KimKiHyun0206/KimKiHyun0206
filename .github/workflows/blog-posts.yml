name: Latest blog post workflow
on:
  schedule:
    # 매일 한국 시간(KST) 00:00에 업데이트 (UTC 15:00)
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Pull in posts
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://v2.velog.io/rss/@duskafka"
          # $index를 사용하여 번호를 명시 (1, 2, 3...)
          # style 속성을 사용하여 파란색 제거 및 색상 고정
          # 📖 이모지 추가
          template: "$index. <a href='$url' style='color: #DDE6ED; text-decoration: none;'>📖 $title</a> ($date)$newline"
          date_format: "yyyy-mm-dd"
          max_post_count: 10
